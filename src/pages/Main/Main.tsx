import styles from './Main.module.scss'

const Main = () => (
  <section className={styles.page} aria-labelledby="main-title">
    <h1 id="main-title">Главная</h1>
  </section>
)

export default Main
